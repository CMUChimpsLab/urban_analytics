Okay in this one we're moving to aggregate by neighborhood instead of just
displaying all the tweets all the time. Really it looks a lot like map1, I
can't really tell the difference.

neighborhoods.json: a GeoJSON file, neighborhoods of Pittsburgh

